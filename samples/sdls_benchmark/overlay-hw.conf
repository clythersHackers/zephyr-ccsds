# Enable the ESP32-S3 AES engine driver. Provider inspection determines
# separately whether the PSA GCM call chain actually reaches this driver.
CONFIG_CRYPTO=y
CONFIG_CRYPTO_ESP32=y
CONFIG_CRYPTO_ESP32_AES=y
CONFIG_CRYPTO_ESP32_SHA=n
